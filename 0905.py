if targetBall_x > whiteBall_x and targetBall_y > whiteBall_y :
    radian = math.atan(height / width)  # tan -1
    angle = 180 / math.pi * radian

    radian = math.atan(height / width)

    radian = math.atan(height / width)